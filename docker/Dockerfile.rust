FROM rust:1.80-bullseye as builder
WORKDIR /app
COPY crates/ crates/
WORKDIR /app/crates
RUN cargo build --release

FROM debian:bullseye-slim
WORKDIR /app
COPY --from=builder /app/crates/target/release/signalintel-core /usr/local/bin/
CMD ["signalintel-core"]
